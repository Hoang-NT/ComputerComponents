
Add this certificate in Postman application to use SSL connection (https)


URL in Postman like:

	https://localhost:8443/api/login

Note that URL must use "https" and port change to "8443" (instead of "8080")


- In Postman go to File -> Settings
- Click tab Certificates, turn on CA Certificates, browse to localhost.crt file in this folder